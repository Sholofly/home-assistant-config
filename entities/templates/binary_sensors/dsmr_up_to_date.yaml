################################################
## Entities / Template Binary Sensors / DSMR up to date
################################################
state: "{{ (now() - as_local(as_datetime(states('sensor.dsmr_reading_timestamp')))) < timedelta(minutes = 1, seconds = 30) }}"
