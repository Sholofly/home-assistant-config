name: "KNMI Weeralarm Gelderland"
unique_id: knmi_weeralarm_gelderland
state: >-
  {{ (state_attr('sensor.knmi_all_data', 'item')
        | selectattr('title', 'search', 'Waarschuwingen Gelderland')
        | map(attribute='description')
        | join).split('.')[0]
  }}
attributes:
  warning: >-
    {{ (state_attr('sensor.knmi_all_data', 'item')
        | selectattr('title', 'search', 'Waarschuwingen Gelderland')
        | map(attribute='description')
        | join).split('.')[1:] | join('.') | striptags | trim
    }}