platform: rest
name: Shopping List
headers:
  authorization: !secret shopping_list_auth
  content-type: "application/json"
resource: !secret shopping_list_url
value_template: >
  {{ value_json  | selectattr("complete", "equalto", false) | map(attribute='name') | list | join('<br>') }}
method: GET
scan_interval: 20
