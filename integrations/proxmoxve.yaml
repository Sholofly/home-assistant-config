################################################
## Integrations / Proxmox VE
################################################
proxmoxve:
  - host: !secret proxmox_ip
    port: !secret proxmox_port
    verify_ssl: false
    username: !secret proxmox_username
    password: !secret proxmox_password
    nodes:
      - node: !secret proxmox_node_name
        vms:
          - 105